import NavBar from '../components/header/NavBar'

function Concept({ navbarProps }) {
    return (
        <div>
            <NavBar {...navbarProps} />
        </div>
    );
}

export default Concept;