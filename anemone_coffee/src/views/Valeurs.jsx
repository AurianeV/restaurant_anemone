import NavBar from '../components/header/NavBar'

function Valeurs({ navbarProps }) {
    return (
        <div>
            <NavBar {...navbarProps} />
            <h1>Nos valeurs</h1>

            
        </div>
    );
}

export default Valeurs;